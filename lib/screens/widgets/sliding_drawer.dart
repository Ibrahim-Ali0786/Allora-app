/// REMOVED in v2. The drawer was replaced by the chat-list header actions
/// (search / networks / settings) and the Settings hub. This file is kept
/// only because the deployment sandbox forbids deleting files; it is no
/// longer imported anywhere.
library removed_sliding_drawer;
